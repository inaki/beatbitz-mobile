import { SELECTED_PATTERN } from '../actions/types';
// import { INAKI } from '../images'

export default patternsReducer = (state = {}, action) => {
    switch(action.type) { 
        case SELECTED_PATTERN:
            return state
        default: 
            return state;
    }
}